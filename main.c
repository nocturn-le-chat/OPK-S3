#include <stdio.h>
#include <math.h>

int main(double a, b, c) {
  printf("A >>> \n B >>> \n; C >>> \n"); scanf("%d", &a); scanf("%d", &b); scanf("%d", &c);
  if a == 0: return c / b;
  if b == 0 and c > 0: return sqrt(-a);
  if b == 0 and c < 0: return sqrt(a);
  if (a == 0 and c == 0) or (b == 0 and c == 0): return 0;
  if 
}
